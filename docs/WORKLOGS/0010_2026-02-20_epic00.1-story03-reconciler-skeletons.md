# Worklog: Epic 00.1 STORY_03 — Reconciler and Provider Skeletons

**Date:** 2026-02-20
**Session:** Implement provider/reconciler/jobbuilder skeletons and full main.go wiring
**Status:** Complete

---

## Objective

Implement STORY_03 from epic00.1-interfaces: declare all provider, reconciler, and
jobbuilder structs (with stub panicking bodies) so that `cmd/watcher/main.go` can wire
the full controller-runtime manager and `go build ./...` passes end-to-end before any
reconcile logic is written.

---

## Work Completed

### 1. Provider skeleton

Created `internal/provider/k8sgpt/provider.go` — `K8sGPTProvider` struct implementing
`domain.SourceProvider` with `panic("not implemented")` stubs for all four methods.
Compile-time assertion present.

Created `internal/provider/k8sgpt/reconciler.go` — empty file (placeholder for
`fingerprintFor()` in epic01-controller/STORY_02).

Created `internal/provider/provider.go` — `SourceProviderReconciler` struct with all
fields from PROVIDER_LLD §5: `client.Client`, `Scheme`, `Log`, `Cfg`, `Provider`.
Stub `Reconcile` and `SetupWithManager` methods that `panic("not implemented")`.

### 2. Controller skeleton

Created `internal/controller/remediationjob_controller.go` — `RemediationJobReconciler`
struct with fields from CONTROLLER_LLD §6.1: `client.Client`, `Scheme`, `Log`,
`JobBuilder`, `Cfg`. Stub `Reconcile` and `SetupWithManager`.

### 3. JobBuilder skeleton

Created `internal/jobbuilder/job.go` — `Config` struct (`AgentNamespace` field),
`Builder` struct, `New()` constructor (returns error if `AgentNamespace` empty),
stub `Build()`. Compile-time assertion against `domain.JobBuilder`.

### 4. Full main.go wiring

Rewrote `cmd/watcher/main.go` with full manager wiring per CONTROLLER_LLD §7:
- Scheme registration: clientgo + batchv1 + v1alpha1 Result + v1alpha1 Remediation
- `config.FromEnv()` with fatal on error
- Logger construction
- `jobbuilder.New()` with error check
- `RemediationJobReconciler.SetupWithManager()`
- Provider loop: `[]domain.SourceProvider{&k8sgpt.K8sGPTProvider{}}` each wrapped by `SourceProviderReconciler`
- Health probes on `:8080` (metrics) and `:8081` (healthz/readyz)
- `mgr.Start(ctrl.SetupSignalHandler())`

### 5. Tests (stub tests only)

- `internal/controller/remediationjob_controller_test.go`: compile-check test and panic verification tests
- `internal/provider/provider_test.go`: equivalent compile-check and panic tests
- `internal/provider/k8sgpt/provider_test.go`: compile-check and panic tests
- `internal/jobbuilder/job_test.go`: `New()` happy/unhappy path tests

---

## Key Decisions

| Decision | Rationale |
|---|---|
| Stub methods `panic("not implemented")` rather than `return nil, nil` | Makes it immediately obvious if a stub is called in a test that should not use it; harder to miss than a silent nil return |
| `jobbuilder.New()` validates `AgentNamespace` at construction time | Fail-fast: better to catch misconfiguration at startup than at first Build() call |
| `SourceProviderReconciler` wraps `domain.SourceProvider` (not specific to k8sgpt) | Per PROVIDER_LLD §5 — enables multiple provider types with a single generic reconcile loop |

---

## Blockers

None.

---

## Tests Run

```
go build ./...
go test -timeout 30s -race ./...
go vet ./...
```
All clean.

---

## Next Steps

epic00.1 is now complete (all five stories done). Move to epic01-controller:

1. **epic01-controller/STORY_01** — Scheme registration and manager setup (may be partially covered by main.go already)
2. **epic01-controller/STORY_02** — `fingerprintFor()` in `internal/provider/k8sgpt/reconciler.go`
3. **epic01-controller/STORY_03** — `SourceProviderReconciler.Reconcile()` — dedup map / list existing RemediationJobs
4. Continue in story order

Read `docs/BACKLOG/epic01-controller/README.md` and each story file before starting.

---

## Files Modified

| File | Action |
|---|---|
| `internal/provider/k8sgpt/provider.go` | Created |
| `internal/provider/k8sgpt/provider_test.go` | Created |
| `internal/provider/k8sgpt/reconciler.go` | Created (empty) |
| `internal/provider/provider.go` | Created |
| `internal/provider/provider_test.go` | Created |
| `internal/controller/remediationjob_controller.go` | Created |
| `internal/controller/remediationjob_controller_test.go` | Created |
| `internal/jobbuilder/job.go` | Created |
| `internal/jobbuilder/job_test.go` | Created |
| `cmd/watcher/main.go` | Rewritten (full manager wiring) |
| `docs/BACKLOG/epic00.1-interfaces/STORY_03_reconciler_skeleton.md` | Updated (Complete) |
